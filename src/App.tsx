import React, { Component } from 'react';
import './App.scss';

export class App extends Component {

  render() {
    const { movies } = this.state;

    return (
      <div className="page">
      </div>
    );
  }
}
